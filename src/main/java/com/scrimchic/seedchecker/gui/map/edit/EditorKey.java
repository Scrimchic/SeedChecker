package com.scrimchic.seedchecker.gui.map.edit;

/** The keys an editor reacts to, independent of how a Minecraft version reports them. */
public enum EditorKey {
    ENTER,
    ESCAPE,
    TAB,
    BACKSPACE,
    DELETE,
    LEFT,
    RIGHT,
    UP,
    DOWN,
    HOME,
    END
}
